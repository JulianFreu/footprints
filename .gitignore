gpx_files/*
tilecache/*
src/api_key.h

# the tokens the import panel mints; every install signs in for itself
garmin_session/
strava_session/

# written by the settings panel; every install has its own
settings.conf

# build output
build/
build-debug/
dist/
footprints
footprints-debug
footprints.exe
footprints-debug.exe
run-tests
run-tests.exe

# editor-local settings
.vscode/
